using MPUIKIT;
using UnityEngine;
using UnityEngine.UI;

public class CenterTextAndIcon : MonoBehaviour
{
    public MPImage circleImage;
    public RectTransform textRect;
    public RectTransform iconRect;
    float offset = -200;

    //void Start()
    //{
    //    // Set the initial position of the text and icon to the center of the circle
    //    Vector3 textOffset = new Vector2(offset / 2.6117f, offset);
    //    //textRect.anchoredPosition= textOffset;
    //    textRect.anchoredPosition= Vector2.zero;
    //    iconRect.anchoredPosition = Vector2.zero;
    //}
}
